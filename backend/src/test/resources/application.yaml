spring:
  application:
    name: backend
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      mode: never

application:
  sample-thresholds:
    unit-weight: 25
    water-content: 100
    shear-strength: 800
  security:
    jwt:
      secret: test-jwt-secret-key-for-testing-only-must-be-at-least-32-characters-long
      expiration-seconds: 3600
    admin:
      username: admin
      password: admin123
